interface Person {
    name: string;
    age: number;
}

const teo: Person = {
    name: 'Teo',
    age: 10
};

// function add(a: number, b: number): number {
//     return a + b;
// }

// console.log(add(4, 5));
